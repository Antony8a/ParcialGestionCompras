import { FiltroCompra2Pipe } from './filtro-compra2.pipe';

describe('FiltroCompra2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompra2Pipe();
    expect(pipe).toBeTruthy();
  });
});
